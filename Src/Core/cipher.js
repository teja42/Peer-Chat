// This module is responible for managing encryption/decryption.

class Cipher {
   decrypt(key,msg){
      return msg;
   }

   encrypt(key,msg){
      return msg;
   }

}

module.exports = () => new Cipher();